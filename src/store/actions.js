import {
  reqOverviewData,
  reqSummaryData
} from '../api'
import {
  Overview,
  Summary
} from './mutation-types'

export default {
  // async getSummary({ commit }) {
  //   const result = await reqSummaryData()
  //   commit(Summary, { result })
  // },
}
